{
  "name": "pency-scraper-api",
  "version": "1.0.0",
  "description": "API para scrapear productos de Pency",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-browsers": "npx playwright install --with-deps chromium"
  },
  "keywords": [
    "playwright",
    "scraper",
    "api",
    "express"
  ],
  "author": "Tu Nombre",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}